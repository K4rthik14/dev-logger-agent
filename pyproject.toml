[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "dev-logger"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
include = ["dev_logger*"]

[project.scripts]
dev-log = "dev_logger.main:app"