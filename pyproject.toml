[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "dev-logger"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
include = ["dev_logger*"]

[project.scripts]
dev-log = "dev_logger.cli:app"