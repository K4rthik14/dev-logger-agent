[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dev-logger"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
include = ["dev_logger*"]

[project.scripts]
dev-log = "dev_logger.__main__:main"